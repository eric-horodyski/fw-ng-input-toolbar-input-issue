import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw-ng-input-toolbar-input-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
